{
  "schema_version": "0.1.0",
  "comment": "Kopiera till runtime.json. Nyckeln ligger i miljön, inte i den här filen. Start-sidan (frontend/start.html) kan skriva en ifylld kopia.",
  "provider": "openai",
  "base_url": "https://api.openai.com/v1",
  "model": "gpt-4.1-mini",
  "api_key_env": "OPENAI_API_KEY",
  "notes": [
    "provider: ollama | openai | anthropic | gemini | openai-compat | hem | chatgpt-oauth | grok-oauth",
    "oauth: ChatGPT/Grok via deras CLI-session. Claude = nyckel, inte OAuth.",
    "openai-compat: valfri base_url (LM Studio, OpenRouter, egen proxy)",
    "Safety i koden körs före och efter. Modellen får inte stänga av den."
  ]
}
